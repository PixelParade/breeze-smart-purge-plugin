---
description: Cloudways MCP for Breeze staging — IDs and project notes (details in wpcp-debug docs/CLOUDWAYS-MCP.md)
alwaysApply: true
---

# Cloudways MCP (Breeze Smart Purge)

**MCP:** `user-cloudways` (global `~/.cursor/mcp.json`)  
**Staging:** `breeze-smart-purge.pixelparade.dev` — server `1305358`, app `6528457`  
**Canonical reference:** `C:\Users\kevin\Projects\wpcp-debug\docs\CLOUDWAYS-MCP.md` (discovery, PHP-FPM, API gaps — do not duplicate here)  
**Staging access matrix:** `docs/ACCESS.md`  
**Global rule:** user rule **Cloudways MCP tool discovery**

## Project-specific

| Task | Tool / note |
|------|-------------|
| Purge cache after deploy | `app_purge_cache` |
| php.ini (`disable_functions` for Novamira) | `server_settings_get` — **not** FPM; prefer SSH WP-CLI |
| PHP-FPM pool | `app_fpm_settings_get` (extended `apps`) |
| Browser wp-admin check | Dashboard SSO (API SSO unresolved Jul 2026) — `ACCESS.md` |
| WP Manager addon | `addon_activate` id `21`; no MCP SSO tool |

Before **unsupported**: run discovery per `CLOUDWAYS-MCP.md`. Novamira binding: `novamira-mcp-safety.mdc`.
