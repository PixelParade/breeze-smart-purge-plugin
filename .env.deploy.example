# Copy to .env.deploy.local and fill in (never commit .env.deploy.local)
# Used for local rsync/scp deploy and pull scripts only.

STAGING_SSH_HOST=45.76.227.59
# Application SSH login (Cloudways Access Details) — NOT the sys_user folder name
STAGING_SSH_USER=cursor-user
STAGING_SSH_PASSWORD=

# Application sys_user / on-disk folder (from .../applications/<this>/public_html)
STAGING_APP_ID=tyaxssmjcp

STAGING_REMOTE_PLUGIN_PATH=/home/1305358.cloudwaysapps.com/tyaxssmjcp/public_html/wp-content/plugins/smart-purge-for-breeze-cache
